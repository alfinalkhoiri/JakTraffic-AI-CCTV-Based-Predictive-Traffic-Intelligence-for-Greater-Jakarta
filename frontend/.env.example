HOST=0.0.0.0
PORT=3000
BROWSER=none

# Deploy cloudflare-worker.js to Cloudflare Workers, then set this URL.
# Leave blank to disable CCTV proxy (streams will show offline state).
REACT_APP_CCTV_PROXY=https://your-worker.workers.dev
